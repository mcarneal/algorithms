const largestInt = require(`./largestInt`)
const binarySearch = require(`./binarySearch`)
const iterableSearch = require(`./iterableSearch`)
const isAlternating = require(`./isAlternating`)
const bracketCount = require(`./bracketCount`)


module.exports = {
    iterableSearch,
    largestInt,
    binarySearch,
    isAlternating,
    bracketCount,
}
